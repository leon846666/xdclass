server:
  port: 8000

spring:
  application:
    name: xdclass-order-service

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.200.129:3306/cloud_order?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: Sk123123/
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.200.129:8848
    sentinel:
      transport:
        dashboard: 192.168.200.129:8080
        port: 9999

#开启feign支持sentinel
feign:
  sentinel:
    enabled: true

# 控制台输出sql、下划线转驼峰
mybatis:
  configuration:
   log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
   map-underscore-to-camel-case: true

xdclass-video-service:
  ribbon:
    NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule